---
id: TKT-029
title: "Disable user revokes active session (isActive checked at session verify) + self-lockout guard"
type: feature
size: small
severity: P1             # P0 (blocker) | P1 (high) | P2 (normal)
group: "admin-polish"
status: merged
assignee: jarvis
branch: feature-TKT-028-users-table-kebab-action
readyToMerge: false
created: 2026-08-15
updated: 2026-08-15
---

## Description

Disabling a user blocks NEW logins (`authenticate` rejects `!isActive`) but
does NOT terminate an EXISTING session: the middleware only verifies the JWT
signature/expiry and never checks `isActive`, so a disabled user keeps working
until the 7-day cookie expires. "Disable" should mean "cannot use the system",
not merely "cannot log in again". Also add a self-lockout guard: an ADMIN can
currently disable their own account or the last active ADMIN.

## Acceptance criteria

- [x] Session verification checks `isActive` for the session's user on
      protected routes (/dashboard*, /admin*); disabled user's existing
      session is rejected on the next request
- [x] Middleware or a small session-verification helper performs the check;
      one indexed primary-key lookup per protected request
- [x] Admin cannot disable their OWN currently-logged-in account
- [x] Admin cannot disable the LAST active ADMIN (at least one active ADMIN
      must remain)
- [x] Login behavior unchanged: inactive users still get the generic
      invalid-credentials response (no account-state leak)
- [x] Unit + integration tests green (session rejection, self-lockout, last-
      admin guard)

## Notes

Gap found during owner review of the user-disable feature (2026-08-15): the
feature already exists end-to-end for login (isActive + setUserActive +
updateUserAction + badge toggle), but has no session revocation and no
self-lockout protection. Pairs with TKT-028 (users-table action menu).

### Verification (2026-08-15)

- [x] `getSession()` (`src/lib/http.ts`) now checks `isUserActive(session.sub)` after JWT verify — disabled user's existing session returns null on the next protected request; dashboard/admin layouts redirect to /login. Middleware stays UX-only (Edge runtime, no Prisma) per architecture doc.
- [x] `isUserActive(id)` added to `src/services/user.service.ts` (indexed PK lookup; false for missing users).
- [x] `setUserActive(id, isActive, { actorId })` guards: SELF_DISABLE (actor === target) and LAST_ACTIVE_ADMIN (no other active admin). `updateUserAction` passes `session.sub` as actorId.
- [x] Login unchanged: `authenticate` still returns null for inactive (generic invalid-credentials, no leak).
- [x] Tests: 8 new integration tests in `tests/integration/user.service.test.ts` (RED first, then GREEN); full suite 342 on tkt028 DB; tsc/lint/build green.

> **done** (2026-08-15): Disable user now revokes active session (isActive checked in getSession); self-disable and last-active-admin guards in setUserActive; 8 new integration tests. Gate green: tsc, 342 vitest, lint, build.

> **merged** (2026-08-15): branch merged into main.
