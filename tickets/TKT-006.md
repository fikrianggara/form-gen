---
id: TKT-006
title: "Multi-rule visibility engine — OR/AND rule sets + validation engine"
type: feature
size: big
severity: P1             # P0 (blocker) | P1 (high) | P2 (normal)
group: rule-engine
status: merged
assignee: jarvis
branch: feature-TKT-006-multi-rule-visibility-en
readyToMerge: false
created: 2026-08-13
createdAt: 2026-08-14T14:02:46+07:00
updated: 2026-08-14
updatedAt: 2026-08-15T17:43:08+07:00
---

## Description

"Multi rule untuk tiap butir pertanyaan, depend ke pertanyaan lain, kondisional
OR dan AND, perlu validation engine": replace the single visibility rule with
multiple rule SETS per question. A question can have several independent rule
sets combined with OR; within a set, conditions combine with AND (or mixed).
A validation engine validates rule references (question exists, type-valid
operators, no cycles, no self-dependency) at save time.

## Acceptance criteria

- [x] Rule schema: multiple rule sets per question, OR between sets, AND/OR within
- [x] Evaluation engine handles nested combinators (unit-tested)
- [x] Validation engine: bad question refs, invalid operators, cycles, self-refs rejected
- [x] Builder UI: rule editor supports adding/removing rule sets
- [x] Public form evaluates correctly (regression suite for existing single-rule forms)
- [x] Unit + integration tests green

### Verification (audit 2026-08-14)

- [x] `VisibilityRule.sets` + `VisibilityRuleSet` in `src/domain/types.ts`; evaluator ORs sets (`src/domain/rules/visibility.ts`), legacy single-set shape still supported.
- [x] `tests/unit/visibility.test.ts` multi-set suite + legacy regression.
- [x] `src/domain/rules/validation.ts` (missing/self refs, operators, value types, allowed deps) + global `detectVisibilityCycles`; wired in `questionnaire.service.ts` for addQuestion/updateQuestionSettings (422 on violation).
- [x] Builder multi-set editor in `src/components/dashboard/Editor.tsx` (add/remove sets, clauses).
- [x] `FormRenderer` uses the same evaluator; legacy rule fixtures in tests still pass.
- [x] 281/281 suite, typecheck, lint, build green on merged main.

## Notes

Shared engine is the foundation for TKT-007 (question blocks) — develop both on
the same branch, engine first.

> **done** (2026-08-14): Multi-rule visibility engine: multi-set rules (OR between sets, ALL/ANY within), validation engine (missing/self refs, operator/value types, allowed deps, global cycle detection), builder multi-set rule editor, duplication remaps rule/aggregate ids. 266/266 tests, lint + build clean.

> **merged** (2026-08-14): branch merged into main.
