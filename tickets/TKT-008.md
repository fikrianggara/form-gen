---
id: TKT-008
title: "AI generation round 2 — flag novel questions, add-to-master modal, admin validation, visibility filter"
type: feature
size: big
severity: P2             # P0 (blocker) | P1 (high) | P2 (normal)
group: ai-sourcing
status: merged
assignee: jarvis
branch: feature-TKT-008-ai-generation-round-2-fla
readyToMerge: false
created: 2026-08-13
updated: 2026-08-17
---

## Description

"Ketika generate form dari AI, flag pertanyaan yang ngga ada di db. User bisa
menambah pertanyaannya sendiri, munculin modal untuk tambah pertanyaan yang sama
di master questionnaire, nanti pertanyaan divalidasi oleh admin kalau mau
dipublish, secara default hanya muncul questionnaire yang dibuat oleh admin dan
yang dibuat sendiri": AI generation must flag questions that do NOT exist in the
master bank (novel AI questions). The user can keep their own questions, and a
modal offers "add this question to the question master". Newly suggested masters
enter a PENDING state, validated by an admin before publish. Master visibility:
by default only admin-created masters and the user's own masters are visible.

## Acceptance criteria

- [x] AI generation distinguishes bank matches vs novel questions (flagged)
      (rag.service: intents with no bank hit at/above the low-confidence
      threshold are NOVEL; returned in result.novel and flagged in the UI —
      browser-verified: "2 new questions flagged below" with a per-question
      Add-to-master button; retrieval excludes PENDING masters, only the
      PUBLISHED bank is matched)
- [x] Modal per novel question: add to master questionnaire (pending state)
      (GenerateForm: per-novel "Add to master" opens a confirm modal →
      addNovelMasterAction persists a QuestionMaster with status PENDING,
      createdBy = session user, auto code — browser-verified:
      what_is_your_favorite_color row created as PENDING + owned, modal shows
      "will be saved as a new question master with status PENDING")
- [x] Master PENDING → PUBLISHED admin validation workflow
      (MastersPanel: PENDING badge + Publish/Reject buttons; publishQuestionMaster
      flips status (and re-embeds), rejectQuestionMaster deletes PENDING only —
      browser-verified: published what_is_your_favorite_color via the admin
      panel, status PENDING → PUBLISHED in DB; integration tests cover
      publish + reject + NOT_PENDING guard)
- [x] Master visibility filter: admin + own by default, published public opt-in
      (visibleMasterWhere: admins see all incl. PENDING; operators see
      published admin/legacy + public opt-in + own masters; setQuestionMasterPublic
      toggles isPublic — integration tests cover admin/operator/other/own/
      public cases; browser-verified "Make public" flips isPublic to true)
- [x] Builder + AI generation surfaces respect the visibility filter
      (edit page passes session viewer to listQuestionMasters; RAG retrieval
      filters status='PUBLISHED'; admin page sees everything — verified by
      integration tests + browser)
- [x] Unit + integration tests green
      (391 tests pass on form_gen_test_tkt008: new suites for master
      visibility/workflow in master-data.service.test.ts, novel detection in
      rag.service.test.ts + rag-provider.test.ts; tsc/lint/build clean)

## Notes

Question-sourcing theme with TKT-009 (external sources). New masters inherit the
versioning + embedding pipeline (auto-embed on create).

> **done** (2026-08-17): AI generation round 2 (TKT-008): novel-question flagging (intents with no bank hit above the low-confidence threshold), add-to-master modal persisting PENDING suggestions, admin PENDING→PUBLISHED publish/reject workflow, master visibility filter (admins see all, operators see published admin/legacy + public opt-in + own), viewer-filtered builder + PUBLISHED-only RAG retrieval. Verified: 391 tests green on ticket DB, tsc/lint/build clean, browser-verified full loop (generate → flag → add → publish → public).

> **merged** (2026-08-17): branch merged into main.
