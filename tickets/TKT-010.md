---
id: TKT-010
title: "Option set nested key extraction from API responses"
type: feature
size: small
group: external-mapping
status: merged
assignee: jarvis
branch: feature-TKT-011-small-wins
readyToMerge: false
created: 2026-08-13
updated: 2026-08-14
---

## Description

"Di option set, untuk response api yang lebih kompleks, bisa nentuin key apa yang
diambil dari response, bisa nested key": for complex external API responses,
let the option-set config pick which keys to extract — including nested keys
(not just the current itemsPath pointer).

## Acceptance criteria

- [x] Nested key support for item list + label + value extraction
- [x] Admin UI: key fields with dot-path preview
- [x] Proxy resolves nested keys (regression on existing external set)
- [x] Unit + integration tests green

### Verification (audit 2026-08-14)

- [x] `OptionSet.apiLabelKey`/`apiValueKey` (dotted, nested) via migration `20260814160000_option_set_api_keys`; `getPath` + `mapOptionItemWithKeys` in `src/domain/options.ts`.
- [x] Label key / Value key inputs in `OptionSetsPanel` with dot-path hints + prefill (external-source only).
- [x] `option-proxy.service.ts` maps items through the keys with graceful fallback to auto-detected keys; regression + fallback integration tests against a stub API.
- [x] `tests/unit/options.test.ts` (9) + `tests/integration/option-proxy.service.test.ts` green; full suite 281/281 on merged main.

## Notes

Small extension of the option-proxy. Share extraction utilities with TKT-009.

> **done** (2026-08-14): Nested API key mapping: OptionSet.apiLabelKey/apiValueKey (dotted paths) applied by the option proxy with graceful fallback to auto-detected keys; admin form fields with prefill. Unit + integration covered. 274/274 tests, lint + build clean.

> **merged** (2026-08-14): branch merged into main.
