---
id: TKT-040
title: "Delete questionnaire — creator-only with response cascade"
type: feature
size: medium
severity: P1
group: "questionnaire-extras"
status: done
assignee: jarvis
branch: feature-TKT-040-delete-questionnaire-cre
readyToMerge: true
created: 2026-08-17
updated: 2026-08-17
---

## Description

Dashboard currently offers View / Responses / Duplicate / Report / Edit for each
questionnaire — no way to remove one. Add a Delete action with a strict
ownership gate: only an ADMIN or the OPERATOR who created the questionnaire may
delete it (stricter than the generic manage gate, which also allows operators to
manage legacy/org-scoped questionnaires). Deleting a questionnaire removes its
responses (DB cascade already wired: Response.questionnaire onDelete: Cascade)
but must NOT touch question masters or option sets — those are shared bank data
referenced by other questionnaires, not owned by this one.

## Acceptance criteria

- [x] Delete questionnaire action exists with permission gate: ADMIN always;
      OPERATOR only when questionnaire.createdBy === session.sub
      (assertCanDeleteQuestionnaire in access-control.service.ts — deliberately
      stricter than the manage gate: operators cannot delete legacy (createdBy
      null) or org-scoped rows; deleteQuestionnaireAction wires it; integration
      tests cover admin-any / own / other's / legacy-strict / anonymous)
- [x] Deleting a questionnaire also deletes all its responses (answers,
      answer groups, audits cascade via Response)
      (DB FK Response.questionnaire onDelete: Cascade + explicit
      response.deleteMany in deleteQuestionnaire; integration test verifies
      response + invitation + join rows all gone)
- [x] Question masters and option sets referenced by the deleted questionnaire
      are NOT deleted (shared bank data survives)
      (masters/option sets are referenced via QuestionnaireQuestion joins which
      cascade from Questionnaire — only join rows die; integration test asserts
      both masters + the option set survive; browser-verified: deleting a
      questionnaire left its q_name/q_email/q_age masters intact)
- [x] Confirmation before delete in the dashboard UI (destructive, irreversible)
      (DeleteQuestionnaireButton confirm() warns the questionnaire + responses
      are removed and masters/option sets are NOT; browser-verified dialog copy)
- [x] Dashboard reflects deletion (revalidate + redirect on success)
      (deleteQuestionnaireAction revalidates /dashboard; button calls
      router.refresh(); browser-verified card disappears after delete)
- [x] Unit + integration tests green
      (414 tests pass on form_gen_test_tkt040: 5 delete-gate cases in
      access-control.service.test.ts, 3 cascade/survival cases in
      questionnaire.service.test.ts; tsc/lint/build clean; browser-verified
      admin deletes, operator rejected on foreign row, operator deletes own)

## Notes

<!-- Completion summary, test results, merge notes — appended by ticket.sh -->
