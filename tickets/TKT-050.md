---
id: TKT-050
title: "Landing hero — fold AI feature into hero with overlapping cards; hover 'Try AI generation' brings AI card to top"
type: feature
size: small
severity: P2
group: "landing-page"
status: backlog          # backlog | ongoing | done | merged | cancelled
assignee: null           # agent name once assigned
branch: null             # feat/TKT-000-slug once started
readyToMerge: false
created: 2026-08-19
updated: 2026-08-19
---

## Description

Owner request (2026-08-19): merge the AI feature into the hero section —
currently the hero and the AI section (TKT-049) are two separate sections.
One hero section; the AI generation card overlaps the questionnaire product
card; hovering the "Try AI generation" trigger brings the AI card to the top
of the questionnaire card (z-order swap with smooth transition).

## Acceptance criteria

- [ ] Single hero section on the landing page — no separate AI section below it
- [ ] AI generation card overlaps the questionnaire product card (layered layout)
- [ ] Hovering the "Try AI generation" trigger brings the AI card to the top of the questionnaire card (verified via class/toggle change in browser)
- [ ] AI copy stays accurate (prompt → hybrid RAG retrieval → draft → flag novel) — no fabricated claims
- [ ] Design consistent with technical-editorial blueprint; responsive; contrast AA; zero JS errors; no horizontal overflow
- [ ] `npx tsc --noEmit` passes; lint + build green

## Notes

Group landing-page. Remove the standalone AiSection from the page once folded
into the hero. Keep the hero a server-safe component; use a small client
island for the hover/toggle behavior if needed.
