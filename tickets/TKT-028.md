---
id: TKT-028
title: "Users table: kebab action menu (Edit, Reset password, Enable/Disable) + pure status badge"
type: feature
size: small
severity: P1             # P0 (blocker) | P1 (high) | P2 (normal)
group: "admin-polish"
status: merged
assignee: jarvis
branch: feature-TKT-028-users-table-kebab-action
readyToMerge: false
created: 2026-08-15
updated: 2026-08-15
---

## Description

The users table's Status badge is a hidden control: it renders as a flat
green/red pill but is actually a clickable button that toggles `isActive`.
There is no affordance — no hover state, no cursor, no tooltip — so admins
don't realize it toggles the account. Owner decision (2026-08-15): replace the
row's scattered inline actions with a **kebab action menu**, same pattern as
the responses table (TKT-017 `ResponseActionsMenu`), and make the Status
column a pure display badge. Enable/Disable is an explicit labeled action
(option b), not a hidden clickable badge.

## Acceptance criteria

- [x] Status column shows a pure display badge (active green / disabled red),
      NOT a clickable control
- [x] Actions column has a kebab button (IconMore) opening a popup menu with:
      Edit, Reset password, Enable/Disable
- [x] Enable/Disable shows an inline confirm state in the menu (like the
      Delete confirm in ResponseActionsMenu) before toggling
- [x] Menu closes on outside click and Escape; matches ResponseActionsMenu
      behavior
- [x] Edit and Reset password keep today's inline row-expansion forms
      (triggered from the menu, not separate buttons)
- [x] Permission gate unchanged (MANAGE_USERS, ADMIN only)
- [x] No behavioral change server-side; tests green

## Notes

Owner decision 2026-08-15: use the popup action-menu pattern (like the
responses page) with option (b) — explicit Enable/Disable, not a clickable
badge. The Status badge being a hidden toggle was discovered by the owner in
review: "it only shows as badges, I dunno if the badge can be clicked".
Reuses the established ResponseActionsMenu interaction; consider extracting a
shared PopupMenu primitive if both tables need identical behavior.

### Verification (2026-08-15)

- [x] Status cell now renders `<Badge>` only (display) — `src/components/admin/UsersPanel.tsx`; toggle moved out of the badge.
- [x] `UserActionsMenu` (kebab IconMore) in the Actions column: Edit / Reset password / Enable-Disable — mirrors `ResponseActionsMenu` (outside-click + Escape close).
- [x] Disable/Enable uses inline confirm state before toggling (red for disable, emerald for enable).
- [x] Edit / Reset password trigger the existing inline row-expansion forms from the menu.
- [x] `IconBan` added to `src/components/icons.tsx` (TKT-018 icon-set style).
- [x] Server unchanged (same `updateUserAction`/`setUserActive`); gate green: tsc, 342 vitest on tkt028 DB, lint, build.

> **done** (2026-08-15): Users table kebab action menu (Edit/Reset password/Enable-Disable) + pure display status badge; IconBan added. Session-revocation + self-lockout in TKT-029. Gate green: tsc, 342 vitest, lint, build.

> **merged** (2026-08-15): branch merged into main.
