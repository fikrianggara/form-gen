---
id: TKT-022
title: "Invitation.responseId as a real FK relation"
type: feature
size: small
severity: P1             # P0 (blocker) | P1 (high) | P2 (normal)
group: "invitation-hardening"
status: backlog          # backlog | ongoing | done | merged | cancelled
assignee: null           # agent name once assigned
branch: null             # feat/TKT-000-slug once started
readyToMerge: false
created: 2026-08-15
updated: 2026-08-15
---

## Description

`Invitation.responseId` is a plain `String?` with no FK — the schema allows
dangling refs and the detach is manual in `deleteResponse`
(`src/services/response-admin.service.ts`). From finding F-009.

## Acceptance criteria

- [ ] Migration: `Invitation.responseId` becomes a proper relation to
      `Response` (nullable, onDelete semantics decided — e.g. SetNull)
- [ ] `deleteResponse` no longer needs the manual detach (cascade handles it),
      or the manual detach stays as defense-in-depth and is verified
- [ ] Existing invitations migrate cleanly (no data loss)
- [ ] Tests cover delete-response → invitation detach

## Notes

Finding F-009 (P1). Owner decision (2026-08-15): proceed. Coordinate with the
response-admin service; small migration.
