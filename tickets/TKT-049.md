---
id: TKT-049
title: "Landing page — highlight AI questionnaire generation (RAG hybrid retrieval)"
type: feature
size: small
severity: P2
group: "landing-page"
status: ongoing
assignee: jarvis
branch: feature-TKT-049-landing-page-highlight-a
readyToMerge: false
created: 2026-08-19
updated: 2026-08-19
---

## Description

Owner request (2026-08-19): update the landing page to highlight the AI
feature in questionnaire generation. Verified feature: `/dashboard/generate`
runs `generateQuestionnaireFromPrompt` (rag.service) — natural-language prompt
→ intent extraction → hybrid RAG retrieval (trigram + vector embeddings over
the question-master bank) → draft questionnaire with title/description,
flagging low-confidence matches and proposing novel questions not in the bank
(TKT-008). Falls back to deterministic provider when embeddings unconfigured.

## Acceptance criteria

- [ ] New AI section on the landing page (between hero and features, or integrated into features) that accurately describes prompt → hybrid RAG retrieval → draft questionnaire
- [ ] Copy reflects real behavior: question bank reuse, novel-question suggestions, low-confidence flagging — no fabricated claims
- [ ] CTA to `/dashboard/generate` (or a "Try AI generation" link)
- [ ] Design consistent with existing technical-editorial blueprint (navy/indigo/paper, mono labels)
- [ ] Responsive (mobile + desktop); contrast AA; zero JS errors; no horizontal overflow
- [ ] `npx tsc --noEmit` passes; lint + build green

## Notes

Group landing-page. Verify the real feature behavior from
src/services/rag.service.ts + src/components/dashboard/GenerateForm.tsx
before writing copy — do not invent capabilities.
