---
id: TKT-066
title: "Copy-to-clipboard for API keys"
type: feature
size: small
severity: P2
group: "api-key-ux"
status: done
assignee: jarvis
branch: feature-TKT-066-copy-to-clipboard-for-ap
readyToMerge: true
created: 2026-08-21
createdAt: 2026-08-21T23:22:50+07:00
updated: 2026-08-22
updatedAt: 2026-08-22T19:43:58+07:00
---

## Description

One-click copy for API keys: a copy button on the key prefix (and on the
one-time secret reveal) in the API keys panel
(`src/components/admin/ApiKeysPanel.tsx`, `src/components/portal/ApiAccessRequestForm.tsx`).

Owner request (2026-08-21): "ada fitur copy key di api key."

## Acceptance criteria

- [x] Copy button next to the key prefix copies `keyPrefix` to clipboard;
      button shows a transient "Copied" state.
- [x] When a newly issued/approved secret is displayed once, a copy button
      copies the full secret (that is the only moment the plaintext exists).
- [x] Uses `navigator.clipboard` with a fallback for insecure contexts; no
      secret is ever stored client-side after the reveal is dismissed.
- [x] Reuses the labeled-action + toast patterns (Fikri UX preference:
      explicit buttons, toast feedback).
- [x] Component test for the copy interaction (mock clipboard API).

## Notes

- Small, isolated; can be picked up any time. The secret-reveal copy is the
  sensitive one — make sure the secret value isn't left in DOM state after
  the modal closes.

> **done** (2026-08-22): Add copy-to-clipboard for API key prefixes and secret reveal with toast feedback and fallback
