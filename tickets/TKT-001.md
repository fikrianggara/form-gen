---
id: TKT-001
title: "Unique-link distribution & lazy response creation — mailblast sample links, no accounts"
type: feature
size: big                # small | medium | big
severity: P0             # P0 (blocker) | P1 (high) | P2 (normal)
group: respondent-auth   # epic/branch group (see tickets/README.md)
status: merged
assignee: jarvis
branch: feature-TKT-001-unique-link-distribution
readyToMerge: false
created: 2026-08-14
updated: 2026-08-14
---

## Description

Re-scoped by owner (2026-08-14): respondents do NOT need accounts. The system
generates a unique link per sample email and mailblasts the link to the emails
listed as the sample (if the questionnaire has one). Clicking the link opens
the public form. Generating links creates NO questionnaire/response data;
a blank Response row is created only when the link is clicked and the
respondent saves it as a draft (or completes).

Implementation plan: `.hermes/plans/2026-08-14_160000-unique-link-distribution.md`.

## Acceptance criteria

- [x] Questionnaire holds an optional sampleEmails list
- [x] Generate-links action creates one Invitation row per email (unique token); NO Response rows yet
- [x] Mailblast sends each sample email the unique link (SMTP when configured, console fallback otherwise)
- [x] Public link validates, marks the invitation clicked, and renders the form
- [x] First save (draft/complete) lazily creates the Response and persists the CURRENT form state atomically (respondentToken=token, respondentLabel=email) — never blank data
- [x] Anonymous flow (no invite) unchanged
- [x] Unit + integration tests green

### Verification (audit 2026-08-14)

- [x] `Questionnaire.sampleEmails Json?` + `Invitation` model (migration `20260814160000_invitations`).
- [x] `src/services/invitation.service.ts`: `generateInvitations` (dedupe/invalid-email filter, no Response rows), `sendInvitations` mailblast with `sentAt`.
- [x] `src/services/mail.service.ts`: SMTP-ready transport with `consoleTransport` fallback.
- [x] `src/app/api/invitations/[token]/route.ts` GET validates + marks clicked; FormRenderer invite flow renders the form with no row created.
- [x] `createResponseWithState` in `src/services/response.service.ts` creates Response + current answers atomically on first save (route POST passes state through).
- [x] Anonymous bootstrap unchanged (POST without answers creates a blank draft as before).
- [x] 297 tests green on ticket DB; tsc/lint/build clean.

## Notes

Supersedes the earlier accounts plan
(`.hermes/plans/2026-08-13_150000-respondent-accounts.md`) — no Respondent
model, no passwords, no requiresAccount gate.

<!-- Completion summary, test results, merge notes — appended by ticket.sh -->

> **done** (2026-08-14): Unique-link distribution (re-scoped, no accounts): sampleEmails on Questionnaire; Invitation model+service (generate/validate/click/link); mail service with SMTP-ready console fallback + sendInvitations mailblast with sentAt; GET /api/invitations/[token] validates+marks clicked; FormRenderer invite flow lazily creates the Response WITH current form state on first save (atomic, never blank); sampleEmails editor UI with generate+send links. 5 commits on feature-TKT-001-unique-link-distribution; gate green (tsc, 297 tests, lint, build).

> **merged** (2026-08-14): branch merged into main.

> **merged** (2026-08-14): branch merged into main.
