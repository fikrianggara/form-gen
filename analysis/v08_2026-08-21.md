# FormGen — System Analysis v08 (2026-08-21)

Analysis: **owner decisions on v07 open questions + ticket re-scoping**.
The owner answered Q1, Q3, Q4, Q5, Q6 in chat (2026-08-21); the affected
tickets were re-scoped with the decisions baked into their acceptance
criteria. Q2 (Google OAuth implementation approach) remains open — P2,
decide when TKT-053 is picked up.

## 1. Decisions recorded

| Q | Question | Owner decision | Impact |
|---|----------|----------------|--------|
| Q1 | Username vs name for registration | **Add a unique `username` column** to User; login accepts username OR email | TKT-051 (schema + signup + login), TKT-059 (homepage shows username, fallback to name) |
| Q2 | Google OAuth implementation | *(unanswered — deferred)* | TKT-053 — decide at pickup: custom OIDC routes vs NextAuth |
| Q3 | Org-admin representation | **Org-role value on OrganizationMember** — `role ADMIN|OPERATOR|DEV` within the org | TKT-055 schema, TKT-056 role changes |
| Q4 | Keep `User.organizationId` shortcut? | **Keep and sync on approve/leave** | TKT-055 — existing org gates unchanged |
| Q5 | Proposal approval atomicity on AI failure | **Keep org+survey; save the questionnaire description; proposer retries generation themselves** | TKT-061 — approval not rolled back; persisted generation intent + retry action |
| Q6 | Conditional-rule UX | **Fix the UX** — user picks dependency question + value; multiple rules (AND/OR) supported | TKT-064 — from verify-only to a rule-builder build ticket |

## 2. Decision → ticket mapping

| Ticket | Re-scope applied |
|--------|------------------|
| TKT-051 | `User.username String @unique` migration + backfill; signup collects username/email/password; login by username or email; tests for both |
| TKT-059 | Note: homepage header uses `User.username`, falls back to `User.name` |
| TKT-055 | OrganizationMember.role = ADMIN/OPERATOR/DEV (org-scoped); org creator = org admin; `User.organizationId` kept + synced on approve/leave |
| TKT-056 | Role changes via `OrganizationMember.role`; org admin cannot grant org ADMIN (only existing org admin/global ADMIN) |
| TKT-061 | Happy path atomic; on AI failure org+survey stay, `pendingQuestionnaire` (or equivalent) persists the generation intent, proposer/admin retries "Generate questionnaire" until it exists |
| TKT-064 | Rule-builder UX: "Show only if…" pick dependency question + operator/value, multi-rule AND/OR rows, persists existing visibilityRule shape; plus end-to-end verification |

## 3. Still open

- **Q2 — Google OAuth implementation** (TKT-053): custom OIDC route pair vs
  NextAuth library. Not blocking: TKT-053 is P2 and independent of the
  current waves. Decision line remains in this register.

## 4. Status

- All affected ticket ACs updated on main (doc commit). Tickets remain
  `backlog`, ready for assignment.
- Register advanced: v07 (immutable) → v08 (this file) → `LATEST.md` updated;
  README version table + ledger rows F-016/F-017 updated.
